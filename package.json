{
  "name": "my-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "My custom OpenClaw test plugin built on the latest microkernel framework",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "node -e \"const fs=require('node:fs'); fs.rmSync('dist', { recursive: true, force: true }); fs.rmSync('artifacts', { recursive: true, force: true });\"",
    "generate:registry": "node ./scripts/generate-registry.mjs ./src/app ./src/generated/registry.ts ./src/app/plugin-config.ts MyPluginConfig",
    "stage:plugin-artifact": "node ./scripts/stage-plugin-artifact.mjs ./dist/app/plugin.manifest.js",
    "sync:plugin-package-json": "node ./scripts/sync-package-json.mjs ./dist/app/plugin.manifest.js",
    "generate:plugin-manifest": "node ./scripts/generate-plugin-manifest.mjs ./dist/app/plugin.manifest.js",
    "validate:plugin": "node ./scripts/validate-plugin.mjs ./dist/app/plugin.manifest.js",
    "build": "npm run generate:registry && npx tsc -p tsconfig.json && npm run stage:plugin-artifact && npm run sync:plugin-package-json && npm run generate:plugin-manifest && npm run validate:plugin",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
